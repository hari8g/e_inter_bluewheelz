# Production (Vercel): set to the BluWheelz Render Web Service URL — origin only, no path.
# Example: https://e-inter-bluewheelz.onrender.com
# Do not use https://e-inter-api.onrender.com (older API, no dashboard login).
# No trailing slash; no quotes in the Vercel dashboard.
# Leave unset for local dev (Vite proxies /api → http://127.0.0.1:8787).
# VITE_API_ORIGIN=https://e-inter-bluewheelz.onrender.com
