# Do not set VITE_API_ORIGIN for Vercel. The app must call same-origin /api/v1
# so vercel.json can proxy to Render. A baked Render URL is cross-origin and
# the browser blocks it when the API omits CORS headers.
